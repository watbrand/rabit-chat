# RabitChat Environment Variables
# Copy this file to .env and fill in the values

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection string
# On Replit: Auto-provisioned when you add PostgreSQL database
# Local development: Use your local PostgreSQL instance
DATABASE_URL="postgresql://user:password@localhost:5432/rabitchat"

# Individual PostgreSQL connection params (auto-set on Replit)
# These are optional if DATABASE_URL is set
PGHOST="localhost"
PGPORT="5432"
PGUSER="postgres"
PGPASSWORD="your-password"
PGDATABASE="rabitchat"

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# Session encryption key - use a strong random string (32+ characters)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="your-super-secret-session-key-change-this-in-production"

# ===========================================
# SERVER (Optional - has defaults)
# ===========================================
# Server port (default: 5000)
PORT="5000"

# Node environment: development | production
NODE_ENV="development"

# ===========================================
# MEDIA UPLOADS (Optional - Cloudinary)
# ===========================================
# Sign up at https://cloudinary.com for free
# Get credentials from Dashboard > Settings > Access Keys
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# ===========================================
# REPLIT-SPECIFIC (Auto-set on Replit)
# ===========================================
# These are automatically set by Replit - do not modify manually
# REPLIT_DEV_DOMAIN - Development domain for CORS
# REPLIT_DOMAINS - Production domains
# REPL_ID - Replit project ID
# EXPO_PUBLIC_DOMAIN - Public domain for API requests
